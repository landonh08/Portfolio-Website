---
const { link, name } = Astro.props;
---

<div class="bg-tertiary w-fit min-h-fit rounded-2xl p-2 m-2 hover:scale-125 transition-all border-hover">
    <img src={link} alt={name} class="w-12 h-12">
</div>